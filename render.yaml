services:
  - type: web
    name: mediacleaner-redact-pro
    env: python
    buildCommand: "./build.sh"
    startCommand: "uvicorn server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
